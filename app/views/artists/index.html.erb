<%@artists.each do |artist|%>
  <%=artist.name%>
  <%= link_to "#{artist.name}", artist_path(artist)%>
  <%artist.songs.all.each do |song|%>
    <ul><%= link_to "#{song.title}", song_path(song)%></ul>
  <%end%>
<%end%>
